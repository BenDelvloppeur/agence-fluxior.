---
import { cn } from "../../lib/utils";

interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<div class={cn("mx-auto w-full max-w-7xl px-5 md:px-10", className)}>
  <slot />
</div>
