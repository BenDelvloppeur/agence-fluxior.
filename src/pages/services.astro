---
import Layout from '../layouts/Layout.astro';
import Container from '../components/ui/Container.astro';
import { Navbar } from '../components/layout/Navbar';
import Footer from '../components/layout/Footer.astro';
import { CheckCircle2, ChevronDown, Smartphone, ShieldCheck, Zap } from "lucide-react";
import { Reveal } from "../components/ui/Motion";
import MagicCTA from "../components/ui/MagicCTA.astro";
import { PricingCardsSwipeable } from "../components/pages/PricingCardsSwipeable";
import { PricingCardsDesktop } from "../components/pages/PricingCardsDesktop";

const FAQ_ITEMS = [
  {
    question: "Combien coûte la création d'un site web ?",
    answer: "Chaque projet est unique, mais nos offres de site vitrine commencent généralement autour de 700€/mois. Pour un e-commerce complet, nos tarifs débutent à 1500€. Nous établissons un devis précis après analyse de votre cahier des charges."
  },
  {
    question: "Quels sont les délais de livraison ?",
    answer: "Pour un site vitrine premium, comptez entre 2 à 3 jours. Pour un projet e-commerce plus complexe, prévoyez 5 à 7 jours. Nous privilégions la qualité et les tests rigoureux."
  },
  {
    question: "Le site sera-t-il optimisé pour Google (SEO) ?",
    answer: "Absolument. C'est notre ADN. Structure sémantique, vitesse de chargement, balisage Schema.org, compression des images... Tout est inclus nativement pour plaire aux moteurs de recherche."
  },
  {
    question: "Puis-je modifier le contenu moi-même ?",
    answer: "Oui ! Nous intégrons un CMS (système de gestion de contenu) moderne et intuitif. Une formation vidéo personnalisée vous est fournie à la livraison."
  },
    {
    question: "Proposez-vous un service de maintenance ?",
    answer: "Oui, nous proposons des forfaits de maintenance mensuelle incluant les mises à jour de sécurité, les sauvegardes quotidiennes et des modifications mineures de contenu."
  }
];

const STACK = [
  { name: "Astro", icon: "🚀" },
  { name: "React", icon: "⚛️" },
  { name: "Tailwind", icon: "🎨" },
  { name: "TypeScript", icon: "📘" },
  { name: "Vercel", icon: "▲" },
  { name: "Stripe", icon: "💳" },
];
---

<Layout title="Nos Services & Tarifs | Agence Fluxior">
  <Navbar client:load />
  
  <main class="pt-32 pb-20 overflow-hidden">
    <Container>
      <!-- Hero Services -->
      <div class="max-w-4xl mb-24 relative z-10">
        <Reveal client:load>
            <h1 class="text-5xl md:text-7xl font-display font-bold mb-8">
              Nos <span class="text-gradient">Services.</span>
            </h1>
        </Reveal>
        <Reveal client:load delay={0.2}>
            <p class="text-gray-400 text-xl leading-relaxed max-w-2xl">
              Des solutions digitales haut de gamme, sans frais cachés.
              Choisissez la formule adaptée à votre ambition et laissez la magie opérer.
            </p>
        </Reveal>
      </div>

      <!-- Pricing Grid -->
      <div class="mb-32 relative z-10">
        <!-- Desktop: Grille normale -->
        <div class="hidden md:block">
          <PricingCardsDesktop client:load />
        </div>
        <!-- Mobile: Swipeable Cards -->
        <div class="md:hidden">
          <PricingCardsSwipeable client:load />
        </div>
      </div>

      <!-- Features Grid (Bento) -->
      <div class="mb-32">
        <Reveal client:visible className="text-center mb-16">
            <h2 class="text-3xl md:text-5xl font-display font-bold mb-6">Tout ce qui est <span class="text-gradient">inclus</span></h2>
            <p class="text-gray-400 max-w-2xl mx-auto">Peu importe l'offre choisie, nous ne faisons aucun compromis sur la qualité technique.</p>
        </Reveal>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
            <div class="p-8 rounded-2xl bg-surfaceHighlight/20 border border-white/5 flex flex-col items-center text-center hover:bg-surfaceHighlight/30 transition-colors">
                <Smartphone class="w-8 h-8 text-primary mb-4" />
                <h3 class="font-bold text-white mb-2">Responsive Design</h3>
                <p class="text-sm text-gray-400">Affichage parfait sur tous les écrans (Mobile, Tablette, Desktop).</p>
            </div>
            <div class="p-8 rounded-2xl bg-surfaceHighlight/20 border border-white/5 flex flex-col items-center text-center hover:bg-surfaceHighlight/30 transition-colors">
                <Zap class="w-8 h-8 text-yellow-500 mb-4" />
                <h3 class="font-bold text-white mb-2">Ultra Rapide</h3>
                <p class="text-sm text-gray-400">Chargement en moins d'une seconde pour un SEO optimal.</p>
            </div>
            <div class="p-8 rounded-2xl bg-surfaceHighlight/20 border border-white/5 flex flex-col items-center text-center hover:bg-surfaceHighlight/30 transition-colors">
                <ShieldCheck class="w-8 h-8 text-emerald-500 mb-4" />
                <h3 class="font-bold text-white mb-2">Sécurité Max</h3>
                <p class="text-sm text-gray-400">Certificat SSL, protection anti-DDoS et sauvegardes.</p>
            </div>
        </div>
      </div>

      <!-- Tech Stack Marquee -->
      <div class="mb-32 border-y border-white/5 py-12">
        <p class="text-center text-sm font-bold text-gray-500 uppercase tracking-widest mb-8">Propulsé par la meilleure stack technique</p>
        <div class="flex justify-center gap-8 md:gap-16 flex-wrap opacity-50 grayscale hover:grayscale-0 transition-all duration-500">
            {STACK.map(tech => (
                <div class="flex items-center gap-2 text-xl font-bold text-white">
                    <span>{tech.icon}</span> {tech.name}
                </div>
            ))}
        </div>
      </div>

      <!-- FAQ Section -->
      <div class="max-w-3xl mx-auto mb-32">
        <Reveal client:visible className="text-center mb-12">
            <h2 class="text-3xl font-display font-bold">Questions Fréquentes</h2>
        </Reveal>
        
        <div class="space-y-4">
            {FAQ_ITEMS.map((item, index) => (
                <div class="group border border-white/10 rounded-2xl bg-surface/30 overflow-hidden transition-all hover:border-white/20">
                    <details class="group p-6 cursor-pointer">
                        <summary class="flex items-center justify-between font-bold text-white list-none">
                            {item.question}
                            <ChevronDown class="w-5 h-5 text-gray-500 group-open:rotate-180 transition-transform duration-300" />
                        </summary>
                        <p class="mt-4 text-gray-400 text-sm leading-relaxed border-t border-white/5 pt-4">
                            {item.answer}
                        </p>
                    </details>
                </div>
            ))}
        </div>
      </div>

      <!-- Magic CTA -->
      <MagicCTA 
        title="Prêt à lancer votre <span class='text-gradient'>projet ?</span>"
        subtitle="Ne perdez plus de temps avec des solutions obsolètes. Passez à la vitesse supérieure dès aujourd'hui."
        badge="Démarrer maintenant"
      />

    </Container>
  </main>

  <Footer />
</Layout>
