---
import Layout from '../layouts/Layout.astro';
import Container from '../components/ui/Container.astro';
import { ArrowLeft } from "lucide-react";
---

<Layout title="Page introuvable | Agence Fluxior">
  <main class="h-screen flex items-center justify-center relative overflow-hidden">
    <!-- Background Abstract -->
    <div class="absolute inset-0 bg-background">
        <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[600px] h-[600px] bg-primary/20 rounded-full blur-[120px] animate-blob"></div>
    </div>

    <Container className="relative z-10 text-center">
      <h1 class="text-9xl font-display font-bold text-white mb-4">404</h1>
      <h2 class="text-3xl font-display font-bold text-white mb-6">Oups, vous vous êtes perdu ?</h2>
      <p class="text-gray-400 text-lg mb-10 max-w-md mx-auto">
        La page que vous recherchez n'existe pas ou a été déplacée. 
        Revenons à l'essentiel.
      </p>
      
      <a href="/" class="inline-flex items-center gap-2 px-8 py-4 bg-white text-black font-bold rounded-lg hover:bg-gray-200 transition-all hover:scale-95 active:scale-90">
        <ArrowLeft class="w-5 h-5" />
        Retour à l'accueil
      </a>
    </Container>
  </main>
</Layout>
